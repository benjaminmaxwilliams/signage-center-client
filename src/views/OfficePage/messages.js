import {defineMessages} from "react-intl";

const messages = defineMessages({
    errorRetrieve: {
        id: "office.page.error.retrieve",
        defaultMessage: "Error retrieving offices"
    },
});

export default messages;